import * as _ from '../libs/underscore.js'

export default class AttributCollection{
    constructor(){
        this._attributs = [];
        hydrateWith("attributs.json");
    }
    
    hydrateWith(jsonPath){
        
    }
}